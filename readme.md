1. The null value defined that there is no value into that variable. But, undefined denotes that the variable is yet to be declared or the definition of that variable is missing.

2. In JavaScript, map() function runs into an array, perform a task like, create an HTML element and returns a new array. But, forEach() runs through an array and do its operation and return nothing.

3. The difference between == and === is, == only comapre the values of both sides, on the otherhand, === compares both value and data type of both side of the operator.

4. The significance of async/await in fetching API are, it makes the code readable and easy to manage. Also try catch method makes the error handling of data loading easier.

5. In Javascript there are three scopes, Global, Function and Block. Global scope denotes a variable is declared out of any block of code or in a function. These variables are accessible from anywhere. But, those variables are declared inside a function is not accessible outside of that function. Then, it comes to block scope, like a variable decalred inside a loop is not accessible outside that code block. 